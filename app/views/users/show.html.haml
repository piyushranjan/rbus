%div{:style => "width: 65%;"}
  %h1 
    = @user.nick
  %ul
  - @user.trips.each do |trip|
    %li
      = trip.description
      %br
      = link_to "#{trip.nearby_trips.count} other trips like this", resource(trip)
      